/** @format */
import React from "react";
import styles from "./styles.scss";

const ExamTitleBar = () => {
	return (
		<div className={styles.examTitleBar}>
			<div className={styles.examName}> </div>
		</div>
	);
};

export default ExamTitleBar;
